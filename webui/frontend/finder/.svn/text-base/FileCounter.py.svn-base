import os

class AllFileCounter:
    def __init__(self, query):
        self.query = query
        self.count = 0

    def find(self, obj):
        self.count = self.count + len(obj.file_names) + 1
